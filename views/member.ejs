<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= member_name %> </title>
    <script src="https://cdn.jsdelivr.net/npm/moment@2.29.1/moment.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.5.1/dist/chart.min.js"></script>
</head>
<body>
    <h1>This is the <%= member_name %> page!</h1>
    <canvas id="myChart" width="800" height="800"></canvas>
    <script> 
        var ctx = document.getElementById('myChart');
        var myChart = new Chart(ctx, {
            type: 'scatter',
            data: {
                datasets: [{
                    label: '# of <%= member_id %>',
                    data: [{x: new Date("2015-3-15 0:00"), y: 2}, 
                            {x: new Date("2015-3-25 0:00"), y: 6},
                            {x: new Date("2015-4-15 0:00"), y: 7},
                            {x: new Date("2015-5-1 0:00"), y: 4}],
                    backgroundColor: 'rgb(255, 99, 132)'
                }]
            },
            options: {
                responsive: false,
                scales: {
                    x: {
                        ticks: {
                            callback: function(value, index, values) {
                                let options = {year: 'numeric', month: 'long', day: 'numeric' };
                                let formatted_date = new Date(value).toLocaleString('en-US', options);
                                return formatted_date;
                            }
                        }
                    },
                    yAxis: {
                        beginAtZero: true
                    }
                }
            }
        });
    </script>
</body>
</html>